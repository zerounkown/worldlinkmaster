namespace WorldLinkMaster.Web.Resources;

/// <summary>
/// Marker type only — has no members. Its sole purpose is to give the shared
/// SharedResource.resx / SharedResource.ar.resx files a type to bind
/// IStringLocalizer&lt;SharedResource&gt; to, since the resources live in a
/// dedicated folder rather than next to a specific class.
/// </summary>
public class SharedResource
{
}
